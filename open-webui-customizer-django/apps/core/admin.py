"""
Django admin configuration for core app.

This app primarily contains abstract base models, so no concrete models
are registered here. However, we can set up custom admin site configuration.
"""

from django.contrib import admin

# Register any concrete models from core app if they exist in the future
# For now, this file exists to ensure the app is properly integrated
